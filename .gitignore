.env.local
.env
backend/.env
backend/.env.local
backend/.auth-secrets.json
node_modules
backend/node_modules
src/convex/_generated
# Legacy 2.0 release package stays on disk but is not committed.
# rexos-2.1-release.tar.gz and rexos-2.2-release.tar.gz are intentionally
# tracked so they appear in the GitHub repository root alongside the
# project (like the 2.0 archive did).
rexos-2.0-release.tar.gz
