/**
 * REX OS user-facing configuration.
 * Edit these values to personalise the dashboard.
 */
export const config = {
  appName: "REX OS",
  version: "2.4.0",
  userName: "Ak", // shown in the greeting
  hostLabel: "rexos-node", // shown in the sidebar status
  repo: "https://github.com/laz2y/rex-os",
};
